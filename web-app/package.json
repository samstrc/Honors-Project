{
  "name": "preapproval-web-app",
  "private": true,
  "version": "1.0.0",
  "description": "Polished web front end for the Home Credit consumer loan preapproval project (React + Express), sitting alongside the original Streamlit app.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
